$("#source").html('<%= escape_javascript(render :partial => "problems/test_source", :locals => { problem: @problem }) %>');
$("#test_list").html('<%= escape_javascript(render :partial =>"test_cases/test_case_list", :locals => { problem: @problem }) %>');
$("#test_case_input").val('');
$("#test_case_output").val('');
$("#codemirror-read").each(function() {
  CodeMirror.fromTextArea($(this).get(0), {
    lineNumbers: true,
    mode: "text/x-csrc",
    readOnly: true,
    scrollbarStyle: "null"
  });
});
